import { HomeSparkles } from "./components/home";

export default async function Home() {
  // const data = await getData();

  return (
    <main className="h-[100vh]">
      <HomeSparkles />
    </main>
  );
}
